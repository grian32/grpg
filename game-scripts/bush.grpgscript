onInteract(BERRY_BUSH) {
    if (getObjState() == 0) {
        setObjState(1);
        playerInvAdd(BERRIES);
        playerAddXp(FORAGING, 100);

        timer(100) {
            setObjState(0);
        }
    }
}
